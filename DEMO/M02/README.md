# M02 — Implement programmability objects

Run `common/01-programmability.sql`, then `local/01-exercise.sql` against `DP800_M02`. The setup creates a reporting view, transactional stored procedure, scalar function, inline TVF, and an audit trigger. These core objects work in SQL Server 2025 and Azure SQL.

