# Azure notes

The common objects use portable Azure SQL syntax. For production, replace broad execution rights with least-privilege grants, use Entra principals where required, and test trigger/procedure concurrency under the Azure service tier selected for the application.

