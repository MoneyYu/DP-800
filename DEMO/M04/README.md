# M04 — AI-assisted SQL workflow

This module does not call a Copilot API. Use `common/copilot-instructions-example.md` as a reviewable instruction example and `local/01-review-target.sql` as the intentionally poor input for an explain/review/refactor exercise. Compare a generated answer with `local/02-reference-improvement.sql`; never paste credentials or production data into a prompt.
