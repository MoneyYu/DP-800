# M07 — CI/CD with SQL database projects

`common/Dp800.Database` is an SDK-style SQL project with object-per-file layout and a passwordless Azure publish profile. Run `local/Build-SqlProject.ps1` to build. `azure/build-deploy.yml` is a sample workflow and contains secret references only.
