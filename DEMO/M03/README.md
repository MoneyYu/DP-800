# M03 — Write advanced T-SQL

Run `common/01-advanced-objects.sql`, then `local/01-advanced-queries.sql` against `DP800_M03`. The local script demonstrates recursive CTEs, windows, JSON, `SOUNDEX`/`DIFFERENCE`, graph `MATCH`, structured errors, and runtime detection of SQL Server 2025 regular-expression syntax.
