# Azure notes

Run the common and query scripts on the target Azure SQL database and record the engine edition/version. Regular-expression functions are version-sensitive, so retain the feature-detection result rather than assuming parity with a local SQL Server 2025 build.
