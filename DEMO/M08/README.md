# M08 — Integrate SQL with Azure services

Run `common/01-product-api.sql` against `DP800_M08`. `common/dab-config.json` exposes tables and a read-only view through REST and GraphQL while reading the connection string from `DATABASE_CONNECTION_STRING`.

Follow `local/README.md` for local DAB and `azure/README.md` for managed hosting differences.

