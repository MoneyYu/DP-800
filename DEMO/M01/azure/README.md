# Azure notes

Run the common script in an Azure SQL database after removing the `USE` assumption. Temporal, JSON, partitioning, and graph are available, but Azure SQL manages storage rather than exposing the same filegroup design choices as SQL Server. Verify service objective, ledger requirements, and preview status in the target database before teaching them.

